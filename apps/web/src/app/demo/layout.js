export const metadata = {
  title: 'MiniMe — A Tale of Two Tuesdays',
  description: 'See how MiniMe gives Selam her Tuesdays back. An animated story about AI that handles your customers while you run your shop.',
};

export default function DemoLayout({ children }) {
  return children;
}
